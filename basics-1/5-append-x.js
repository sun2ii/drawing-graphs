const { ListNode }    = require('../../ds');
const { linkedList1 } = require('../../testCases');
const { toArray } = require('../../utils');

/* Append an Element to a Linked List (iterative1 / recursive2) */

const appendElement1 = (list, x) => {
}

const appendElement2 = (list, x) => {
}

console.log(appendElement1(linkedList1, 7));
console.log(appendElement2(linkedList1, 9));
