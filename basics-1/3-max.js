const { linkedList1 } = require("../../testCases")

/* Find Max Element in a Linked List (iterative / recursive) */

let maxElement1 = list => {
}

let maxElement2 = list => {
}

console.log(maxElement1(linkedList1));
console.log(maxElement2(linkedList1));
